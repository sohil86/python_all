1. The "source" folder has the source code (file_4.c) and the object file (file_4.o).

2. The "include" folder has all the header file (header.h).

3. The "build" folder contains the Makefile.

4. The "bin" folder contains the executable file.

5. To run the executable on command line, go to bin folder and write ./file_4 <file name> OR ./file_4 <destination>/<file name>.
